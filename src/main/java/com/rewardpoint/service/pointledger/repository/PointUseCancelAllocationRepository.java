package com.rewardpoint.service.pointledger.repository;

import com.rewardpoint.service.pointledger.entity.PointUseCancelAllocation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PointUseCancelAllocationRepository extends JpaRepository<PointUseCancelAllocation, Long> {
}
